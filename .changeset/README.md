# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to add a changeset describing what you changed in which
package and at what semver bump. The release workflow rolls them up into a
single Version PR; merging it publishes packages to npm.
